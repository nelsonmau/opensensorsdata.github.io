---
layout: null
---
<?xml version="1.0"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ site.data.settings.title }}</title>
    <link>{{ site.data.settings.url }}</link>
    <atom:link href="http://example.com/rss.xml" rel="self" type="application/rss+xml" />
    <description>{{ site.data.settings.description }}</description>
    <language>it-it</language>
    <pubDate>{{ site.data.settings.time | date: "%a, %d %b %Y %H:%M:%S %z" }}</pubDate>
    <lastBuildDate>{{ site.data.settings.time | date: "%a, %d %b %Y %H:%M:%S %z" }}</lastBuildDate>

    {% for post in site.posts limit:50 %}
    <item>
      {% if post.layout == "link" %}
        <title>{{ post.title }}</title>
        <pubDate>{{ post.date | date: "%a, %d %b %Y %H:%M:%S %z" }}</pubDate>
        <author>contact@opensensorsdata.it (opensensorsdata crew)</author>
        <guid>http://archeostickers.com{{ post.id }}</guid>
        <description><![CDATA[ {{ post.content }} <p><a href="http://www.opensensorsdata.it{{ post.url }}">★</a></p>]]></description>
      {% else %}
        <title>{{ post.title }}</title>
        <link>http://www.opensensorsdata.it{{ post.url }}</link>
        <pubDate>{{ post.date | date: "%a, %d %b %Y %H:%M:%S %z" }}</pubDate>
        <author>contact@opensensorsdata.it (opensensorsdata crew)</author>
        <guid>http://www.opensensorsdata.it{{ post.id }}</guid>
        <description>{{ post.content | xml_escape }}</description>
      {% endif %}
    </item>
    {% endfor %}

  </channel> 
</rss>